<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NewsMonitor - Semantic Feed Aggregator</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            line-height: 1.6;
            color: #333;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        header {
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            margin-bottom: 30px;
        }

        h1 {
            color: #667eea;
            font-size: 2.5em;
            margin-bottom: 10px;
        }

        .subtitle {
            color: #666;
            font-size: 1.1em;
        }

        .stats {
            display: flex;
            gap: 20px;
            margin-top: 20px;
            flex-wrap: wrap;
        }

        .stat-box {
            background: #f8f9fa;
            padding: 15px 25px;
            border-radius: 8px;
            border-left: 4px solid #667eea;
        }

        .stat-number {
            font-size: 2em;
            font-weight: bold;
            color: #667eea;
        }

        .stat-label {
            color: #666;
            font-size: 0.9em;
        }

        .feeds-section {
            background: white;
            padding: 25px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            margin-bottom: 30px;
        }

        h2 {
            color: #667eea;
            margin-bottom: 20px;
            font-size: 1.8em;
        }

        .feed-list {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 15px;
        }

        .feed-card {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 8px;
            border-left: 4px solid #764ba2;
            transition: transform 0.2s;
        }

        .feed-card:hover {
            transform: translateX(5px);
        }

        .feed-title {
            font-weight: bold;
            color: #333;
            margin-bottom: 5px;
        }

        .feed-link {
            color: #667eea;
            text-decoration: none;
            font-size: 0.9em;
            display: block;
            margin-bottom: 5px;
        }

        .feed-link:hover {
            text-decoration: underline;
        }

        .feed-meta {
            color: #999;
            font-size: 0.85em;
        }

        .entries-section {
            background: white;
            padding: 25px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }

        .entry {
            border-bottom: 1px solid #e9ecef;
            padding: 20px 0;
        }

        .entry:last-child {
            border-bottom: none;
        }

        .entry-title {
            font-size: 1.4em;
            margin-bottom: 10px;
        }

        .entry-title a {
            color: #333;
            text-decoration: none;
            transition: color 0.2s;
        }

        .entry-title a:hover {
            color: #667eea;
        }

        .entry-meta {
            color: #666;
            font-size: 0.9em;
            margin-bottom: 10px;
        }

        .entry-author {
            color: #764ba2;
            font-weight: 500;
        }

        .entry-date {
            color: #999;
        }

        .entry-content {
            color: #555;
            line-height: 1.7;
        }

        footer {
            text-align: center;
            color: white;
            margin-top: 30px;
            padding: 20px;
        }

        .powered-by {
            font-size: 0.9em;
            opacity: 0.9;
        }

        .tech-stack {
            margin-top: 10px;
            font-size: 0.85em;
            opacity: 0.8;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>📰 NewsMonitor</h1>
            <p class="subtitle">Semantic Feed Aggregator powered by SPARQL & Transmissions</p>

            <div class="stats">
                <div class="stat-box">
                    <div class="stat-number">{{ templateData.feeds | length }}</div>
                    <div class="stat-label">Active Feeds</div>
                </div>
                <div class="stat-box">
                    <div class="stat-number">{{ templateData.entries | length }}</div>
                    <div class="stat-label">Total Entries</div>
                </div>
                <div class="stat-box">
                    <div class="stat-number">✅</div>
                    <div class="stat-label">Deduplication Active</div>
                </div>
            </div>
        </header>

        <div class="feeds-section">
            <h2>📡 Subscribed Feeds</h2>
            <div class="feed-list">
                {% for feed in templateData.feeds %}
                <div class="feed-card">
                    <div class="feed-title">{{ feed.currentItem.title.value }}</div>
                    <a href="{{ feed.currentItem.link.value }}" class="feed-link" target="_blank">{{ feed.currentItem.link.value }} →</a>
                    {% if feed.currentItem.numItems %}
                    <div class="feed-meta">{{ feed.currentItem.numItems.value }} items</div>
                    {% endif %}
                </div>
                {% endfor %}
            </div>
        </div>

        <div class="entries-section">
            <h2>📝 Latest Entries</h2>

            {% for entry in templateData.entries %}
            <div class="entry">
                <h3 class="entry-title">
                    <a href="{{ entry.currentItem.link.value }}" target="_blank">
                        {{ entry.currentItem.title.value }}
                    </a>
                </h3>
                <div class="entry-meta">
                    {% if entry.currentItem.creator %}
                    <span class="entry-author">by {{ entry.currentItem.creator.value }}</span>
                    {% endif %}
                    {% if entry.currentItem.date %}
                    {% if entry.currentItem.creator %} • {% endif %}
                    <span class="entry-date">{{ entry.currentItem.date.value }}</span>
                    {% endif %}
                </div>
                {% if entry.currentItem.content %}
                <div class="entry-content">
                    {{ entry.currentItem.content.value | truncate(300) }}
                </div>
                {% endif %}
            </div>
            {% endfor %}
        </div>

        <footer>
            <div class="powered-by">
                <strong>Powered by NewsMonitor</strong> - A production-ready feed aggregator
            </div>
            <div class="tech-stack">
                🔧 Tech Stack: Transmissions Framework • Apache Jena Fuseki • SPARQL • RDF • SIOC & Dublin Core Vocabularies
            </div>
            <div class="tech-stack">
                ✅ Features: Multi-format parsing (RSS/Atom/JSON) • GUID-based deduplication • Semantic Web standards
            </div>
            <div class="tech-stack" style="margin-top: 15px;">
                Generated: {{ now() }} • Total Sources: {{ templateData.feeds | length }} feeds • Entries: {{ templateData.entries | length }}
            </div>
        </footer>
    </div>
</body>
</html>
