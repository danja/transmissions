
import { Process } from '../'../services/Process';

// Test for Process
console.log('Testing Process...';

try {
    const instance = new Process();
    console.log('Process Test Passed';
} catch (error) {
    console.error('Process Test Failed:', error.message);
}
